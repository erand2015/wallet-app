import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lex.app', // Mund ta lësh siç është ose ta ndryshosh
  appName: 'Lex Associates', // Emri që do shfaqet në telefon
  webDir: 'out' // KJO DUHET TË JETË 'out'
};

export default config;